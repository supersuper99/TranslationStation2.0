import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TranslationStation2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
